Mogao Content Studio 0.1.4 fixes image copying in Xiaohongshu mode and adds actual image clipboard actions.

- **复制图文正文** now keeps rich text and embedded images for Xiaohongshu long-form drafts. Title copying remains separate.
- Add **复制这张图** beside each image and an image picker under **复制图片**. These actions write PNG image bytes, including conversion from JPEG/WebP/AVIF/GIF (GIF is static).
- Add **复制卡片 N** below each card, copying text and illustrations together as a 1080 × 1440 PNG.
- Use rich Clipboard API copying with a native-selection compatibility fallback. Sanitize copied content, wait for images, preserve cursor/selection and report failures.
- Preserve the larger editor, source notes, templates, DOCX/image exports and network protections. No platform upload or automatic publication is added.

Validation: official Obsidian ESLint and CSS checks pass with zero warnings, TypeScript passes, and 80 offline tests pass. Local browser clipboard/paste checks received two embedded images in rich text and real PNG file payloads for individual images/cards. Actual platform acceptance remains unverified; some editors filter embedded image data. Save temporary workbench edits before reloading the plugin.
