# Mogao Content Studio 0.1.19

- 在 Obsidian 的插件设置页新增「支持开发 → 查看打赏二维码」，打开即可使用微信扫码。
- 二维码内置于 `main.js`，离线可看；仍只需安装 `main.js`、`manifest.json`、`styles.css` 三个文件。
- 兼容 Obsidian 1.13 的可搜索设置页和旧版设置页。打赏完全自愿。
- 中英文 README 顶部保留直达打赏区域的链接，并补充插件内入口说明。

更新时替换三个插件文件并重新加载插件，保留 `data.json`，原设置和自定义模板不变。配套浏览器助手仍为 0.3.8，无需更新。

验证：Obsidian lint、CSS lint、类型检查和 122 项插件测试通过；已在本机 Obsidian 1.13.7 加载 0.1.19，点击设置页入口并确认二维码完整显示。安装只替换三个插件文件，核对 `data.json` 未被改写。

本版只增加自愿支持入口，平台同步的验证范围沿用 [0.1.18 说明](0.1.18.md)。
