# Mogao Content Studio 0.1.16

修复“能提取，但不像原文”的一组原因：正文采样从段落标签深入实际文字节点，避免把附注小字当成正文；保留字间距、首行缩进、空行间距、标题外层底色/边框和主容器样式。标题前景与背景从同一组观测样式选取，避免拼出白底白字。相对字号、行高与 CSS important 优先级参与采样；不混合未激活的条件样式。

学习模板使用简洁基础样式，不再混入“墨白”的边线、绿色引用框等装饰。修复渲染时边框简写覆盖引用左边线的问题。增加“样式示例 / 当前稿件”切换，展示标题、正文、强调、引用与图片占位；不再以提取属性数量暗示视觉还原程度。

请重新提取链接后保存，新算法不会自动重写已有模板。参考文章图片仍跳过，不改变笔记中的原图或发布流程。浏览器助手仍为 0.3.7，无需重新加载。

Validation: nested WeChat-shaped fixtures cover typography, containers, heading contrast, relative units, spacer paragraphs, preservation of source prose, neutral fallbacks and sanitization. A local browser comparison with identical synthetic text showed matching body size/color/line height/letter spacing, blue heading background, white heading text, article frame and quotation border. The previously downloaded reference exposed the old 14px body selection versus the actual nested 17px text; the updated sampler selects 17px.

The latest reported WeChat URL could not be opened by browser automation because of site-safety policy. It has not been visually verified against the new template. The local comparison is regression evidence, not a claim of pixel-identical reconstruction of that article. Image-based ornaments, responsive/conditional designs and complex layouts remain outside the static template model.
